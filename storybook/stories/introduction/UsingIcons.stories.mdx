import { Meta } from '@storybook/addon-docs/blocks';

<Meta title="Introduction/Иконки"
      parameters={{
        previewTabs: {
          canvas: {
            hidden: true,
          },
        },
      }} />

# Иконки

**Содержание:**
+ [Общая информация](#общая-информация)
+ [Полезные ссылки](#полезные-ссылки)
+ [Использование иконок](#использование-иконок)
+ [Мок иконок в тестах](#мок-иконок-в-тестах)

<br/>

## Общая информация

Иконки используются из единой билиотеки @fe/icons - [Storybook](http://icons.prod.a.o3.ru/?path=/story/icon--all-icons)

Единая библиотека иконок - это синхронизированные между собой пространство дизайнеров Figma Core Icon и репозиторием [@fe/icons](https://gitlab.ozon.ru/fe/icons)

Единая библиотека иконок - это один источник истины для разработчиков и дизайнеров всех платформ и направлений: Web Platform (FE, BX, SX), Mobile Platform (iOS, Android)

За счет формализованных процессов добавления и изменения иконок, а также за счет автоматизированного процесса синхронизации кода и дизайна, мы добиваемся:

- актуальности иконок в библиотеке
- консистентности между кодом  и дизайном в проекта

## Полезные ссылки

**Документация**: [🖼 Единая библиотека иконок](https://confluence.ozon.ru/pages/viewpage.action?pageId=230159449)

**Дизайн**: [🥨 Core - Icons](https://www.figma.com/file/Ns3cTNtQSI8HVAV47bC9I5/%F0%9F%A5%A8-Core---Icons?node-id=2735%3A940)

**Чат поддержки:** [~fe-icons](https://msg.o3.ru/ozon/channels/fe-icons)

## Использование иконок

Импортируем нужную иконку в компонент
```js
import { ic_m_search as IconSearch } from '@fe-icons';
// or
import IconSearch from '@fe-icons/components/ic_m_search';
````
Доступен и динамический импорт:
```js
require('@fe-icons/components/ic_m_search').default
````

Локально регистрируем ее в компоненте
```js
export default {
  name: 'OzComponent',
  components: { IconSearch }
}
```
Используем в `template` компонента
```html
<template>
  <div class="component">
    <IconSearch/>
  </div>
</template>
```
Меняем цвет через `css`
```html
<template>
  <div class="component">
    <IconSearch :style="{ color: 'blue' }"/>
  </div>
</template>
```

## Мок иконок в тестах

Т.к. в наших проектах мы часто собираем бандлы в es5: а иконки из @fe/icons собраны в es6, то во время тестирования есть необходимость мокать иконки.

Если вам нужно проверить какой-то класс, аттрибут или просто наличие иконки в тестах вам следует сделать мок по примеру:

```js

jest.mock('@fe-icons', () => ({
  ic_m_search: 'svg',
}));
```
На snapshot-ах это будет выглядеть примерно так:

```js
<div ><svg class=\\"icon\\" qa-id=\\"search-icon\\"></svg></div>
```
